@model IEnumerable<Theke.Models.Category>


<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <div class="row">
        <div class=".col-sm-4">
            <table class="table">
                <tr>
                    <th>

                    </th>

                    <th></th>
                </tr>


                @foreach (var item in Model)
            {
                    <tr>
                        <!--IST DASSELBE WIE ACTIONLINK <p><a class="btn btn-default" href="#">@Html.DisplayFor(modelItem => item.Name)</a></p> -->

                        @Html.ActionLink(item.Name, "Index", null, new { @class = "btn btn-primary" })
                    </tr>
                }

            </table>
        </div>
    </div>
</body>
</html>
